import styled from 'styled-components';

import {colors} from '@/styles/shared';

export const statisticsRow = styled.div`
  padding-bottom: 5px;
`;

export const statisticsValue = styled.strong`
  color: ${colors.white};
`;
