import styled from 'styled-components';

import {actionListContainer} from '@/styles/shared';

export const actionIconsListContainer = styled.div`
  ${actionListContainer};
`;

export const tooltipWrapper = styled.div`
  margin: 0 6px;
`;
